# Server secrets — copy to .env and fill in (never commit .env)
PORT=3000
CONTROL_PASSWORD=
BROADCASTER_SECRET=
API_SECRET=
REGISTRATION_ENABLED=true

# WebRTC TURN (coturn) — same password as in /etc/turnserver.conf
TURN_USER=roborubiks
TURN_PASSWORD=
TURN_URLS=turn:roborubiks.ru:3478,turn:130.49.143.78:3478
